type rec tree =
  | EmptyTree
  | TreeNode({value: int, left: tree, right: tree})

let binarySearchTree = _ => panic("'binarySearchTree' is not implemented")
let sortedData = _ => panic("'sortedData' is not implemented")
